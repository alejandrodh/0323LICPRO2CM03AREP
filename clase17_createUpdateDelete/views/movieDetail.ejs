<!DOCTYPE html>
<html>

<head>
  <title>UdeSA Movies</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1>UdeSA Movies</h1>
  <%- include('./partials/nav.ejs') %>
  <h2><%= movie.title %></h2>
  <ul class="content">
      <li>Rating: <%= movie.rating %></li>
      <li>Awards: <%= movie.awards %></li>
      <li>Genre: <a href="/genres/id/<%= movie.genre.id %>"><%= movie.genre.name %></a></li>
  </ul>
  <!-- Opción 1 para pasar los datos de id al controlador: usar una ruta por post que tenga como parámetro el id. En el controlador lo recuperamos con req.params.id -->
  <!-- <form action="/movies/delete/id/<%= movie.id%>" method="POST">  -->

  <!-- Opción 2 para pasar los datos de id al controlador: usamos una campo oculto que tenga el id del elemento a borrar. El dato debe viajar dentro del atributo value de un campo oculto (type="hidden"). En el controlador lo recuperamos con req.body.nameDelCampoInput -->
  <form action="/movies/delete/" method="POST">
    <input type="hidden" name="id" value="<%= movie.id%>">
    <button type="submit">Borrar</button>
  </form>
</body>

</html>